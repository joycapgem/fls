public class temp {
    public static void sample(){
        system.debug(system.url.getOrgDomainUrl());
        //system.debug(system.Url.toExternalForm());
        system.debug(URL.getSalesforceBaseUrl().toExternalForm()); 
		system.debug(System.UserInfo.getSessionId());
    }
}